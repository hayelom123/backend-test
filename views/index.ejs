<!DOCTYPE html>
<html lang="en">
<%- include('./partials/header.ejs') %>
<body>

    <h1><%= title %></h1>
    <%- include('./partials/nav.ejs') %>
    <p>welcome to <%= title %></p>
    <% if (blogs.length>0){ %>
         <% blogs.forEach((blog)=>{  %>
             <h3> <%= blog.name %> </h3>
             <h4> <%= blog.age %> </h4>
             <p> <%= blog.desc %> </p>

            <% }) %>

        <% } %>
</body>
</html>